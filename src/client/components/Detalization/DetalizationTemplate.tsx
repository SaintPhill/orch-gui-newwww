import React from 'react';

import './Detalization.scss';

export function DetalizationTemplate(): JSX.Element {
    const ROOT_CLASS = 'detalization';

    return (
        <div className={ROOT_CLASS}>
            Detalization
        </div>
    );
}
